const currentPet = require('./currentPet');
const addPet = require('./addPet');
const removePet = require('./removePet');
const currentUser = require('./currentUser')

module.exports = {
  currentPet,
  addPet,
  removePet,
  currentUser,
};